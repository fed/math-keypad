import React from 'react';
import MathKeypad from '../src';

export default {
  title: 'Math Keypad'
};

// Basic keypad
export const basic = () => <MathKeypad />;

basic.story = {
  name: 'Basic'
};
